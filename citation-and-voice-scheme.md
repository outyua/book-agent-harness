# 引用分层与去 AI 味：研究记录与方案

> 成文 2026-08-20。输入：全书 25 个 markdown 稿件（17 章 + 序 + 后记 + 6 篇部扉页）通读抽样 + 引用密度机器统计 + 现有体例（`editorial-standards.md`、后记 C、`verify-citations.sh`、Chromium 分页构建链）。
> 2026-08-20 用户已确认三条取舍：①叙述句出处进章末注，对照表/代码题注的 `file:line` 保留；②抽出后补机制与判断，禁止用总结句填空；③先改第 1、6 章当样章，看过再铺开。
> 同日作者纠正：书的重点不是让读者做认证核查，而是结合项目源码把知识讲清楚。三层引用只对了一半——打断继续搬走；把读者当核对员、把 `file:line` 当卖点，作废。裁决见 `manuscript/.review/adjudications.md`，方针红线一改为「讲课优先」。

---

## 0. 结论

两个问题是同一件事的两面：**正文同时在做三份工作**——讲机制、给出处、交代核查方法。后两份把第一份挤瘦了，读感像研究笔记，不像书。

处理原则：

1. **证据不删，分层。** 这本书的护城河就是 `file:line` 与证据分级，全部丢掉会变成又一篇综述。
2. **正文只承担「判断、机制、代价」。** 出处、口径、核日期、URL、存档路径、推算链，退出句子。
3. **腾出来的注意力用来写机制，不拿来补正确的废话。** 句短了不等于内容瘦了；该补的是「为什么选这条、不选的代价、你该抄哪一段」。
4. **去 AI 味的主战场不是禁用词。** 禁用词表已经生效。真正的味道来自「核查笔记渗进正文」和「每章同构开场」。

硬约束：当前书是 Chromium 分页（Playwright），不是 LaTeX。页脚脚注在这条链上不稳定。出处的落点应是**章末注**，不是页脚。

---

## 1. 现状：数字

全书约 41 万字符。与阅读打断直接相关的标记：

| 种类 | 全书次数 | 最密的章 |
|---|---|---|
| `file:line` 形式的源码引用 | 651 | 第 7 章 67、第 2/14 章各 62 |
| `[核 日期]` / `[06-01 基准]` | 292 | 第 6 章 39、第 7 章 31 |
| 超过 40 字的中文括号夹注 | 481 | 第 7 章 43、第 6 章 41 |
| 「出处 / 厂商自述 / 论文实测 / 本书测量」等字样 | 297 | 第 9、16、17 章 |
| arXiv 号 | 169（含后记书目） | 第 9、12、17 章 |
| 引用块（`>`，多为出处或口径） | 175 | 第 7 章 21、第 6 章 18 |

密度最高的章（每千字 `file:line`）：第 14 章 2.63、第 7 章 2.52、第 2 章 2.46。第 12、13、16、17 章源码引用少，但论文/厂商材料的全题名夹在句子里，打断方式不同、程度相当。

对照表、代码块后的路径行、章末基准块——这些查阅面里的引用**不是问题**。问题在连续叙述的段落：一句话里同时塞机制、路径、行号、核日期、证据级别、测量口径。

---

## 2. 现状：三类「引用」其实不是一类东西

后记已经有 C.1 论文书目、C.2 实践者文章、B 项目索引。各章末已有「本章基准」。机器脚本已校验 651 条路径。缺的不是书目，是**正文不肯把已经入书目的东西交给书目**。

把正文里的「引用」拆开，其实是三种职责：

| 种类 | 现在长什么样 | 该留在正文吗 |
|---|---|---|
| **定位符** | `` `goose/crates/goose/src/agents/agent.rs:1849` `` | 对照表、代码块题注、清单：留。叙述句：退出，改由章末注或表承接 |
| **书目** | 「Kelly Hong, Anton Troynikov & Jeff Huber, *Context Rot…*, 2025-07-14, trychroma.com/…」 | 否。首次用短名，全书用后记 C |
| **核查笔记** | 「三点必须交代：标题写 15 个模型，正文表格实为 16 个……」「口径注（这个 21 是哪 21 个）」 | 否。进章末注。诚实边界本身保留，位置换掉 |

第一种是这本书独有的，不能学普通技术书那样只留作者年。第二种、第三种是普通书也会处理的，现在全部堆在夹注和引用块里。

---

## 3. 一个完整病例

第 6 章 §6.1 连续三段，把三种职责叠在一起。先是判断：

> Manus 团队公开分享的生产数据（厂商自述，原文只说「平均」，未给测量方法、样本区间与模型；Manus 是通用 agent 不是 coding agent）是输入输出 token 比约 **100:1**……

紧跟一整段出处块：作者全名、文章全名、日期、URL、仓库存档路径、存档行号、联网返回码、两句英文原文对照。然后再跟一整段推算链：100:1 怎么折成「十倍左右」、价差日期、输出单价假设、命中率不足时比值回落、本书没有真实会话命中率。

读者要的句子其实只有两句：

1. agent 的钱几乎全花在预填充上，缓存命中和未命中可以差一个数量级。
2. 所以后面讲压缩、记忆、检索，先得服从前缀匹配这条铁律。

其余全部是真的、必要的、但不该挡在这两句前面。第 1 章 §1.1 同一结构：先写「剩下的 90% 以上就是本书要讲的东西」，括号里立刻声明这是推算、重述推算链、再交叉引用第 1.4 节。判断刚出口，核查笔记就把它拆掉。

第 9 章更典型。一句「全文没有给出任何一个开始劣化的 token 阈值」后面，括号里是四位作者、报告全名、站点、非同行评审声明、GitHub 测量代码、核日期。括号比判断长。

这不是「引用太多」，是**判断和出处抢同一条视线**。

---

## 4. 方案：三层，而不是「拆成脚注」或「统一成书目」二选一

用户问的两条路——拆出注解、统一引用——都要，但分给不同种类。只做一条会出事：

- **只统一进后记 C**：651 条 `file:line` 进不了普通参考文献；读者对着书目找不到 `query.ts:1157`。
- **只拆成页脚脚注**：Chromium 分页的页脚脚注不稳定；而且核查笔记动辄 80–200 字，页脚装不下。
- **只从正文删掉**：书会变成「各家都做了前缀不变」，读者无法核。护城河没了。

### 4.1 第一层 · 正文（读）

句子里只留：项目名、机制、数字、判断、不成立的条件。

允许留在叙述句里的最短标记：

- 项目名（goose、Claude Code、opencode）
- 论文/文章**短名**，且一章内首次出现时给一次：「AHE 论文」「Manus 那篇实践者文章」「Chroma 的 context rot 报告」。再次出现只用短名。
- 证据级别用**一处、几个字**：厂商自述、论文实测、本书测量。不在同一句里再跟作者、题名、URL。
- 对照表、代码块、验收清单、伪代码里的 `file:line` **原样保留**。这些本来就是查阅面。

禁止再出现在叙述句里的：

- 完整 `path/to/file.ext:123-456`
- `[核 YYYY-MM-DD]`、`[06-01 基准]`（核日期集中到章末基准块；Claude Code 快照在章首或章末声明一次，不逐句贴）
- URL、存档路径、`docs/…` 底稿路径
- 「三点必须交代」「口径注」「判定方法＝」这种核查口吻
- 作者全名 + 题名 + 年份 + arXiv 号同时出现

叙述句里需要指向出处时，用上标注号 `[^6-3]`，指向本章章末注。Markdown 源里写标准脚注语法，构建时渲染为章末「注」而不是页脚（见 §4.4）。

### 4.2 第二层 · 章末注（核）

每章在「版本与复核」**之前**加一节 `### 本章注`。一条注只干一件事，三种模板：

**定位符注**（短）：

```
[^6-12]: goose `crates/goose/src/agents/cache_semantics.rs:21-22` [核 2026-08-18]
```

**书目注**（中）：

```
[^6-1]: Yichao 'Peak' Ji，《Context Engineering for AI Agents: Lessons from Building Manus》，2025-07-18。厂商自述，通用 agent，原文只写「平均」。全文与存档见后记 C.2。
```

**口径注**（长，一条一题，不塞进段中引用块）：

```
[^1-2]: 「循环占 2%–10%」是四个包的测量；「剩下的 90% 就是本书要讲的」是推算。测量命令见 §1.6。推算链：……
```

同一出处在一章里被引多次，只建一条注，后文复用同一注号。Manus 那篇文章第 6 章现在写了三次全文，应收成一条。

章末注不是新的「基准块」。基准块继续管 commit、日期、外部规格；注管正文里被抽走的那一句出处。两者职责分开，避免读者在两个清单之间找同一条。

### 4.3 第三层 · 后记书目（查）

C.1 / C.2 / B 项目索引保留。正文短名必须能在 C 里解到完整条目。现在的毛病是 C 已经有了，正文还把 C 的字段再抄一遍。

改完之后的检验：任意叙述句，删掉所有注号之后，句子仍然完整、仍然下判断。注号不是句子的语法成分。

### 4.4 构建与校验

- 稿件用 Markdown 脚注 `[^章-序号]`。构建脚本把一章内的脚注收集到该章 `本章注`，PDF/EPUB 都做章末注，不做页脚。这样不依赖 Chromium 的 `float: footnote`。
- `verify-citations.sh` 的扫描范围扩到章末注：定位符从正文挪走之后，脚本必须仍能抽到全部 `file:line`。漏扫等于把护城河拆了还不知道。
- 交叉引用「见第 N 章 §N.M」留在正文，这不是书目，是导航。

---

## 5. 「更丰满」指补机制，不是把句子写长

抽出引用之后，段落会变短。短是预期，不是缺陷。要补的是现在被出处挤掉的那一类内容，不是把短句拉回原长度。

每抽出一处，改稿人问三句。答得上就写进正文，答不上就保持短，禁止用过渡句把空洞填回去：

1. **为什么这家这么做？** 现在大量段落停在「X 在 `file:line` 做了 Y」。Y 说完了，选择的原因没有。
2. **不这么做会怎样？** 失败代价现在集中在第 4 段；第 2 段的对照里常常没有。
3. **你该抄哪一段、不要抄哪一段？** 第 5 段有最小实现，但第 2、3 段的逐条对照很少当场下这个判断。

反例（禁止）：抽出出处后写「由此可见缓存非常重要」「这再一次说明了 harness 的价值」。这是用正确的废话填空，比引用打断更糟。

正例。第 2 章现在有一句「这八个实现的主循环没有一个用递归」，后面立刻是普查口径和方法。抽出口径之后，正文应该把那句判断写完：递归把恢复、熔断、插话的挂载点吃掉了，所以八家各自用 `while` 加状态对象——这不是风格偏好，是第 5 章、第 4 章能长在循环上的前提。口径进注。判断留在正文并写完。

---

## 6. 去 AI 味：真正要改的不是词表

`editorial-standards.md` §3.3 的四层框架仍然成立。禁用词表已经扫过，全书几乎没有「赋能 / 抓手 / 众所周知」。机器能抓到的句式也不多：「不是 X 而是 Y」23 处，其中大半是在纠正读者会有的误读，这是本书的工作，不该禁。

味道来自下面四层。按伤害排序。

### 6.1 核查口吻当正文（伤害最大，与 §4 同源）

标志：口径注、三点必须交代、判定方法＝、承重位置、按这条口径、本书未独立复现（同一章重复多遍）。

这些信息要保留——前言的诚实边界是这本书的信用。但它们属于注和前言，不属于段中。同一条诚实声明在前言写一次、相关章的注里回指一次，够了。不要每出现一个数字就当场重读一遍方法论。

### 6.2 判断出口立刻自我拆台

标志：加粗短句 + 长括号把短句降级为推算/转述/未复现。

改法：短句保留；限定条件用后一句普通陈述写完（「这是上界，不是实测」），细节进注。不要用括号把刚刚说的话再解释一遍。

### 6.3 每章同构开场

「读完这一章，你能……」出现 15 次，来自主编手册的五段结构，不是模型随机生成，但读起来像模板。前言已经说明每章固定结构。章首改成**这一章特有的那句判断**，学习目标若还要，收成一句，不要段末朗诵。

「不做对会怎样」的三条清单可以留——那是具体后果，不是模板套话。

### 6.4 对仗与定义链过密

「三个限定词各排除一类东西」「既不是 A 也不是 B，而是 C」。偶用有效，一节里连续三组就会变成节奏而不是论证。改稿时按 §3.3 句式层：有把握就直接断言；没把握写明边界，不靠对仗。

不在这次清单里的：术语先白话后名词（保留，这是纪律不是 AI 味）；反面证据段（保留）；数字必须有出处（保留，出处改到注里）。

---

## 7. 改稿时的操作顺序（给 reviser 的工作单）

单章不要同时「抽出引用」和「重写判断」混成一次语感润色。分两步，否则出处会在改写中丢失。

1. **抽。** 叙述句里的定位符、书目、口径，逐条搬进 `本章注`，正文留短名或注号。对照表和代码题注不动。核日期从句中删除，确认基准块已覆盖。
2. **补。** 对抽空的句子问 §5 的三句。能补机制、代价、抄或不抄，就补。不能补，保持短。
3. **扫味道。** 删同构「读完这一章」；把自我拆台的括号改成后一句或注；核查口吻只许出现在注和基准块。
4. **机器。** 跑 `verify-citations.sh`（扩展后）。注里的 `file:line` 必须仍全部可解析。

第 12、13、16、17 章以书目短名化为主（源码引用本来就少）。第 2、6、7、14 章以定位符外迁为主。不要用同一套力度切所有章。

---

## 8. 落地顺序（已确认）

这是全书体例变更，不是逐句润色。先用两章定模板，再铺开，避免 17 章各写各的注。

1. ~~用户确认本方案的取舍~~（2026-08-20 已确认，见文首）。
2. 总编辑把确认结果写入编辑方针：读者仍是「正在造或正在调 agent 的工程师」；新增「三层引用」和「去核查口吻」为内容红线。
3. **样章**：第 1 章（书目型打断的代表）+ 第 6 章（定位符 + 核查笔记的代表）。改完请用户看样，不看过关不上其余章。
4. 样章确认后再进其余 15 章 + 序/后记的审改循环。`verify-citations.sh` 与构建脚本的章末注收集，在样章阶段一起改，不要留到全书改完。

序和后记的处理：序里关于「带 `file:line`，可以自己去核」的说明要改成「对照表和章末注给出 `file:line`」。后记 C 的导语补一句：正文用短名，完整题名以本表为准。

---

## 9. 明确不做什么

- 不把这本书改成无引用的随笔。没有定位符，选题不成立。
- 不引入边注。6×9 开本外侧 16 mm，放不下 Tufte 式旁注。
- 不把「不是 X 而是 Y」设成禁用。纠正误读是正文工作；只限制一节内连用。
- 不在抽出引用后用总结句把段落填满。
- 不动五段结构的职责顺序（问题 → 对照 → 判据 → 反面 → 最小实现）。改的是段内的信息分层，不是章的骨架。
- 不把诚实边界写软。厂商自述、未复现、样本有偏，全部保留，只是从段中搬到注和前言。
