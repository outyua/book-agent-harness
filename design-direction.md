# 电子书视觉改版记录

## 2026-08-19 · 第一轮样张

本轮先做样张，不直接重排全书。用户指出四个问题：封面不像经典动物技术书、字体不对、全书颜色过强、中文字距偏大，以及 LaTeX 没有得到数学排版。

### 已核到的机制

1. 正文 `body` 使用 `text-align: justify`。Chrome 为了让每行左右对齐，会把中文字符间距拉开；CSS 并没有显式设置较大的 `letter-spacing`。改版样张统一改为左对齐、零字距、零词距。
2. 旧版用了砖红封面大色块、黑色作者底栏、深色部扉页、米色表格和引用块。颜色同时承担了太多层级，阅读页不够安静。
3. 旧版标题声明了 `font-weight: 760/800`。中文字体并没有对应的真实字重，浏览器会合成粗体。样张只使用已安装的真实常用字重：宋体 Regular、Noto Sans CJK SC Medium/Bold、Alibaba PuHuiTi Medium/Bold。
4. 正文当前有两处块级公式，内容都是 `$$\text{Agent} = \text{Model} + \text{Harness}$$`。旧构建器把它当普通段落。本轮引入 Temml 0.13.4，在构建期把 TeX 转成原生 MathML；PDF 与 EPUB 不依赖在线脚本。

### 样张方向

| 方案 | 封面 | 正文 | 用途判断 |
|---|---|---|---|
| A · 图鉴白 | 暖白纸、黑字、一道暗红标记 | 宋体正文 + 普惠体标题 | 最接近自然史图鉴，温和，但技术书识别稍弱 |
| B · 经典色带 | 单一低饱和暗红色带、白底动物版画 | 全黑体、灰阶信息层级 | 最接近经典动物技术书，正文略偏产品文档 |
| C · 书刊混排 | 黑白书刊封面、极少量暗红 | 宋体正文 + Noto 黑体标题 | 长文阅读最稳，封面的“动物书”识别弱于 B |

第一轮的目标是判断视觉方向，不是决定所有细节。比较图在 `proofs/round-1/comparison.png`，六页 PDF 在 `proofs/round-1/book-design-proof-round-1.pdf`。

## 2026-08-19 · 第二轮收敛稿

用户进一步明确参照是 O’Reilly 经典动物技术书，而不是泛指自然史图鉴。第二轮采用第一轮 B 的封面结构与 C 的正文结构，同时避免复制 O’Reilly 商标或某一本现成封面。

### 封面

- 顶部暗砖红色带、中央暖白动物版画区、底部窄黑作者栏。
- 羊缩小并完整露出四肢，四周保留白场，不再顶满页面。
- 顶部色带约占封面高度 23.6%，底栏约占 7.4%；合计约 31%，低于旧封面约 47.9% 的大色块占比。
- 书名、副标题、作者信息统一使用真实存在的中文黑体字重；副标题不再混用宋体。
- 不放 O’Reilly 名称、商标或其他出版机构标识。

### 正文

- 宋体负责连续阅读，Noto Sans CJK SC Medium 负责标题、表格和运行页眉，等宽体只用于代码。
- 正文左对齐，`letter-spacing: 0`、`word-spacing: 0`，不再用两端对齐拉伸汉字。
- 页面只保留黑、灰、暖白和一处暗砖红；引用、表格与代码背景进一步减淡。
- 公式由 Temml 在构建期转为 MathML。样张中的 `Agent = Model + Harness` 已正确排版，不再显示原始 `$$...$$`。

第二轮包含一张封面、一张正文综合压力页和两页 PDF：

- `proofs/round-2/cover-d.png`
- `proofs/round-2/interior-d.png`
- `proofs/round-2/book-design-proof-round-2.pdf`

本轮仍未重建 463 页全书。确认方向后，再把相同规则落到 PDF、EPUB、字体嵌入和数学 manifest 中，避免每次视觉微调都重跑整本书。

## 2026-08-19 · 全书重排成品

用户确认第二轮方向，并要求再缩小一点行距。全书据此重建，排版参数与 `publication-typesetting-standard-review.md` 对齐。

### 落地参数

- 6×9 英寸竖版开本；内侧 22 mm、外侧 16 mm、上 18 mm、下 20 mm，左右页使用镜像装订边距。
- 正文 10.5 pt，行距 1.60，左对齐；不再使用两端对齐扩张中文字距。
- 正文用 Noto Serif SC，标题、目录、表格、页眉用 Noto Sans SC；两套字体均以 OFL 授权的随书子集嵌入 PDF 与 EPUB。
- 章节标题使用 24 / 16 / 12.5 / 10.5 pt 四级层次；正文粗体保持宋体字族，不再让每个强调词切换成黑体。
- 颜色只保留正文黑、辅助灰、暖白和暗砖红。部扉页改为白底，引用块和代码块只用浅灰底，表格采用必要横线而不是全网格。
- 目录改为单栏并压入一页；两张极宽图使用横向图版页，正文仍保持竖版。
- 两处 `$$...$$` 在构建期由 Temml 转为原生 MathML；EPUB 对含公式的两章声明 `mathml` 属性。
- PDF 使用 CSS 分页边栏生成镜像页眉与页码，保留 Chromium 的 tagged PDF 和 outline；不再通过 pypdf/reportlab 叠加页眉页码。
- EPUB 写入结构化目录、替代文本、MathML 与可访问性元数据；PDF 专用的 `@page` 规则在 EPUB 打包时剥离。

### 成品验收

- PDF：444 页，主要页面为 6×9 英寸；2 张超宽图为横向图版页；Tagged = yes；49 个顶层 outline 条目、562 个总 outline 节点；正文中 2 个 MathML、0 个原始 `$$`。该页数为 2026-08-20 正文更新后重新构建的结果。
- EPUB：18 张 SVG 图、2 个嵌入字体、2 个 MathML 章节；EPUBCheck 5.3.0 为 0 error / 0 warning。
- 目检页面覆盖封面、目录、部扉页、章首页、表格与公式页、普通长文页、横向图版页，以及同时包含公众号和个人微信二维码的作者页。

## 2026-08-20 · 封面再设计与细节打磨

在既定经典动物技术书骨架的基础上，对封面进行微观版式与装帧质感深度打磨：

1. **色块与经典色标（Bands & Accent Tab）**：
   - 顶部暗砖红（`#843B34`）色带增加顶部细微反光线（`#9A4A42`）与底部深色阴影线（`#6A2D27`）。
   - 色带左下沿恢复并精细化 O'Reilly 标志性的暗红小色标（Accent Tab，宽 200px、高 14px，带独立边框），打破水平切线的单调感。
2. **中央版画区古典画框与图注（Plate Window & Botanical Caption）**：
   - 在中央暖白纸基上增加双层古典版画框（`#D6CFBE` 与 `#E8E2D4` 细线），四角增加经典十字定位标（Corner Ticks），彻底消除动物插图的悬浮感。
   - 羊版画下方添加精细衬线图注：`—— 美利奴羊 · Ovis aries ——`，字距与水平引线严格对齐，强化自然史与工程纪实质感。
3. **文字字阶与光学对齐（Optical Alignment & Micro-typesetting）**：
   - 主标题英文 `Agent Harness` 字距 `-0.02em`，X 轴向左微调 2px 实现西文 A 与汉字左缘的光学对齐。
   - 中文 `工程` 增加 `0.05em` 字间距，中英行距与上下垂直留白优化为黄金分割比例。
   - 底部作者栏 `王吕 著` 采用主次字阶混排，`电子版 · 2026` 增加字距，顶缘加入沉稳的深灰分割线。
4. **资产与脚本沉淀**：
   - 输出高分辨率成品至 `assets/cover.png`（1600×2400）。
   - 编写专用渲染脚本 `scripts/render-cover.mjs`，同步更新 `styles/cover.css`。

## 2026-08-20 · 正式读者版编辑

这一轮不改变已确认的封面、字体、字号与色彩系统，重点处理出版物与内部研究稿之间的边界。

- PDF 与 EPUB 不再收录 17 个章末「版本与复核」、仓库克隆脚本、机器校验说明、保鲜期维护表和内部稿件路径。
- 正文保留对论证有用的源码文件名、代码位置、论文、规范版本和公开网址；这些属于技术引用，不按内部操作说明处理。
- 前言改为资料范围与时效说明，后记收敛为术语表、项目索引、参考文献、未覆盖内容与资料截止日期。
- `manuscript/` 继续保存研究材料；构建器在 `.build/publication-manuscript/` 生成清理后的正式读者版排版稿，再由同一份排版稿构建 PDF 和 EPUB，避免两种格式内容漂移。
- 正式读者版 PDF 为 384 页，较包含内部复核附录的上一版减少 60 页；17 个章末复核区全部移除。PDF 保留 tagged 结构、2 个 MathML 和 18 张图；EPUBCheck 5.3.0 为 0 error / 0 warning。
