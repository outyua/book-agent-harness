# 主编手册 · 体例与验收标准

> 本书的写作要求。任何一章交稿前按 §7 的清单自检。
> 立于 2026-08-17。

---

## 1. 这本书是什么，不是什么

**是**：面对同一个工程问题，23 个真实 coding agent 分别采用了什么方案、为什么做出不同选择、选择方案时应检查哪些条件，以及哪些实现可以直接采用。（共调研 28 个项目，其中 5 个未在正文留下引用；按照前言的统计规则，不计入「本书基于」的数字。）

**不是**：框架教程（不教 LangGraph 怎么用）、prompt 技巧集（不给可复制的咒语）、论文综述（不按文献年份组织）、产品评测（不排名）。

**目标读者**：正在开发或调试 agent 的工程师。他有能力阅读源码，需要了解其他项目面对相同设计问题时采用了什么方案，以及各方案的代价。他不需要基础的 LLM 概念介绍。

**读者读完应该能做的事**：面对自己 agent 中的任一设计问题，能够列出可选方案、各自的失败模式，并根据项目约束做出选择。

---

## 2. 章节体例：五段结构

每章严格五段，顺序不可变；第 1–3 段标题**按内容拟定**（不用「这一环解决什么」「各家怎么做」这类功能性裸标签做显示标题），第 4、5 段用全书统一名。

| 段 | 职责 | 硬性要求 |
|---|---|---|
| **1 · 这一环解决什么** | 用大白话把问题讲清楚 | 不出现未解释的术语；必须说明「不解决会怎样」 |
| **2 · 各家怎么做** | 源码对照 | 每条论断带 `file:line`；至少覆盖 4 个项目；有对照表 |
| **3 · 分歧在哪、如何选择** | 把选择变成可以直接检查的条件 | 每项判断标准必须能回答是或否，不能只写「视情况而定」 |
| **4 · 反面证据与失败模式** | 打脸的证据 | **篇幅不得少于第 3 段的一半**；必须有至少一条推翻常见直觉的内容 |
| **5 · 可以直接采用的最小实现** | 交付内容 | 数据结构 / 接口签名 / 伪代码 / 参数初值 / 验收测试，至少包含三项 |

每章末尾附**「版本与复核」**一节：本章「基准」块 +「哪些会过期、怎么自己复核」，含可执行的复核命令。

### 2.1 章末「基准」块（强制，置于「版本与复核」节）

> 2026-08-17 编辑复审起，基准块从章首移至章末：版本元数据属于复核语境，不该挡在每章叙事的第一屏。

```
| 项 | 值 |
|---|---|
| 素材复核日期 | YYYY-MM-DD |
| 底稿 | docs/ 下的来源文件与其成文日期 |
| 项目 commit | 本章引用的每个项目的短哈希与日期 |
| API 规格基准 | 厂商文档版本日期 |
```

### 2.2 行内时效标记

- `[核 YYYY-MM-DD]`——已对照当日源码确认。
- `[YYYY-MM-DD 基准]`——源自较旧快照，未能对照最新代码。
- 无标记——原理性内容，不随版本变化。

**必须标记的**：某项目的当前实现、某 API 的价格/限额/参数名、某协议的版本号、任何「目前只有 X 家这么做」式的断言。

---

## 3. 语言要求

沿用 `AGENTS.md`，此处细化为可执行条款：

1. **不发明术语。** 确需一个没有现成名称的概念时，用 `[ ]` 标注并声明「AI 新词」，给出完整定义与所依据的真实理论。全书目前允许的新词：无。
2. **不用隐喻。** 不把甲事物塞进乙事物的语义规则里。「上下文腐烂」这类已在文献中固定的译名可用，但首次出现须给出英文原名与出处。
3. **术语先白话后名词。** 例：「模型读一段文字时会留下中间结果……这份中间结果的技术名称是 KV cache」。
4. **引用理论附原作者与英文原名。** 例：Eric Horvitz, *Principles of Mixed-Initiative User Interfaces*, CHI 1999。
5. **零黑话、零正确废话。** 禁用词表见 §3.1。
6. **英文原文引用保留原文**，不翻译代码注释——注释的措辞本身是证据。中文解释跟在后面。
7. **不用省略实际含义的短词。** 不使用「判据、落地、兜底、骨架、护栏、逃生舱、铁律、锚点、一等公民」等要求读者自行补全含义的表达。根据上下文写成「判断标准、实施步骤、默认处理、伪代码结构、范围检查、完整结果读取入口、必须满足的限制、对应代码位置、独立资源」。
8. **句子要写清动作与对象。** 标题和正文都应尽量写出谁执行什么动作、作用于什么对象、在什么条件下发生，以及结果是什么。不能为了句子短而省略主语、宾语或限制条件。

### 3.1 禁用词表

赋能、抓手、闭环（作动词时）、生态位、范式转移、深度融合、全方位、颠覆式、护城河（正文中；方案文档里可用）、「众所周知」、「不难看出」、「显而易见」、「业界普遍认为」（除非附出处）。

### 3.2 数字纪律

- 任何数字必须有出处，且注明是**厂商自述**、**论文实测**、**源码注释里的估算**还是**本书推算**。
- 不写「大幅提升」「显著下降」，写具体值与对照条件。
- 厂商自报的收益数字必须同时给出它的适用条件。

### 3.3 AI 味检查（四层框架）

AI 味的本质不是某些词本身，而是**用语言的形式感代替内容的信息量**：句子流畅、有结构、显得全面，但删掉之后读者不损失任何东西。统一检查方法是：**删掉这句，读者会损失什么信息？如果答不上来，就删除这句话。**

| 层级 | 特征 | 检查方法 / 替代做法 |
| :--- | :--- | :--- |
| 词汇层 | 无实指的词（见 §3.1 禁用词表；另含「高效、优雅、无缝、极大提升」类万能褒义词） | 经不起「具体指多少、相比什么、代价是什么」追问的词，换成事实与数值（见 §3.2） |
| 句式层 | 「不是 X，而是 Y」「既要 A 也要 B」对仗强迫；「值得注意的是」空洞引导；「在一定程度上」「某种意义上」无边界对冲；「这不仅是……更是……」假升华 | 偶用不是问题，高频出现即是在用节奏感掩盖论证缺口。有把握就直接断言给依据；没把握就写明不成立的边界（「样本量 <1000 时成立」），不模糊过去 |
| 结构层 | 万物三段式（首先/其次/最后）、每段等长、每个要点配加粗小标题、开头复述任务、结尾总结升华 | 篇幅由内容轻重决定：重要的多写，次要的一句带过；结论开头直接给，结尾不复述 |
| 内容层 | 正确的废话：常识包装成洞见（「选择工具取决于具体场景」）；面面俱到但拒绝下判断 | 给明确推荐 + 理由 + 不成立的条件，不写「各有优劣，视情况选择」。这层伤害最大——规避了写作的核心任务：做出判断并承担风险 |

该框架已嵌入审改循环：manuscript-reviewer 维度 5 按此检测（内容层定 P1，词汇句式层零星 P2、成片 P1），manuscript-reviser 与 technical-author 改写时执行对应正面纪律。

---

## 4. 证据分级

正文里每条论断按此分级，且级别必须能被读者看出来：

| 级 | 类型 | 表述方式 |
|---|---|---|
| A | 源码实证 | 附 `file:line`，可直接核 |
| B | 代码注释里的自述 | 引原文，注明「注释称」 |
| C | 官方文档 | 附文档名与基准日期 |
| D | 论文实测 | 附论文英文名与作者 |
| E | 厂商博客自述 | 明写「厂商自述」，不当事实 |
| F | 本书推断 | 明写「推断」，给出推理链 |

**禁止把 E 当 A 用。** 这是本类书最常见的失信点。

---

## 5. 图例规范

- Graphviz DOT 生成 SVG，`.dot` 与 `.svg` 一同入库，放 `figures/`。
- 命名 `ch{章号}-{序号}-{slug}.dot|.svg`。
- 单色安全：不靠颜色区分语义，颜色只做强调；转灰度后仍可读。
- 一张图只讲一件事。超过约 15 个节点就拆。
- **正文必须有一段文字复述图的结论**——图挂了不影响阅读。
- 图不重复正文已说清的内容；图的价值在于展示**空间关系或时序关系**。

---

## 6. 跨章一致性

### 6.1 术语表（全书统一，见 `manuscript/ch99-back-matter.md`）

| 中文 | 英文 | 定义位置 |
|---|---|---|
| 执行框架 | harness | 第 1 章 |
| 代理循环 | agent loop | 第 2 章 |
| 会话运行时 | session runtime | 第 2 章 |
| 前缀匹配 | prefix matching | 第 6 章 |
| 断点 | cache breakpoint | 第 6 章 |
| 插话 / 排队 | steer / queue | 第 4 章 |
| 事件溯源 | event sourcing | 第 14 章 |
| 上下文隔离 | context quarantine | 第 12 章 |

术语首次出现处负责定义，后续章节直接用并回指章号。

### 6.2 交叉引用

- 引用他章写「见第 N 章」，不写页码。
- 同一机制在多章出现时，**只在一处展开**，其余处一句话带过并回指。
- 全书结尾做一次交叉引用校验（§7.3）。

### 6.3 项目称呼

统一用仓库名。Claude Code 是闭源产品，正文不引用其源码、不出现任何 `file:line`；每章基准块用一行写明「闭源，依据 Anthropic 官方文档与工程博客及本书对其公开行为的观察，证据级别为厂商自述与本书观察」，正文首次出现时可加「（本书观察）」，不写「source map 还原」。其余项目直接用仓库名。

---

## 7. 交稿验收清单

### 7.1 单章自检

- [ ] 章末「版本与复核」节有「基准」块，commit 与日期齐全
- [ ] 五段齐全，第 4 段篇幅 ≥ 第 3 段的一半
- [ ] 第 2 段覆盖 ≥4 个项目，全部带 `file:line`
- [ ] 第 3 段的每项判断标准都可以直接检查（能回答是/否）
- [ ] 第 4 段有 ≥1 条推翻直觉的内容
- [ ] 第 5 段有 ≥3 类交付物
- [ ] 末尾有「哪些会过期、怎么复核」+ 可执行命令
- [ ] 所有版本敏感事实带 `[核 ...]`
- [ ] 所有数字有出处与证据级别
- [ ] 无禁用词
- [ ] 新术语先白话后名词
- [ ] 图有文字复述

### 7.2 引用校验（机器可跑）

```bash
# 抽取本章全部 file:line，逐条验证文件存在
grep -oE '`[a-zA-Z0-9_./-]+\.(ts|py|rs|go|swift|kt|tsx)(:[0-9]+(-[0-9]+)?)?`' ch*.md
```
**注意**：文件存在不等于结论成立。抽查时必须重读代码段落本身。

### 7.3 全书终校

- [ ] 术语表与正文一致，无同义异名
- [ ] 交叉引用的章号正确
- [ ] 同一机制无重复展开
- [ ] 各章「基准」块的 commit 日期跨度 ≤ 一个月，否则重新统一复核
- [ ] 前言的「诚实边界」与正文实际做到的一致

---

## 8. 全书的诚实边界（写进前言，不得淡化）

1. 本书的证据是**设计正确性**证据，不是**效果**证据。记录的是各家源码怎么写、注释怎么解释，不是「这样写效果好多少」。
2. 缺生产运营数据：没有失败复盘、没有成本实测、没有长周期稳定性数据。
3. 缺国内第一方工程实践。
4. 样本有偏：28 个项目全部是能拿到源码的，闭源商业 agent 只能靠公开材料推断。
5. 时效有限：见每章末节。

---

## 9. 稿件状态

见 [`manuscript/STATUS.md`](manuscript/STATUS.md)。
