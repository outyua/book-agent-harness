# 审稿报告：ch01-model-plus-harness.md（第 1 轮）

## 结论
不达标
P0：0　P1：12　P2：2

## 总体评价
五段齐全，立论、对照、判据、反面证据、建造顺序都已经写到读者能动手的粒度；技术断言与后记 C 可对上，无未决存疑。本轮不达标几乎全是引用分层尚未执行：叙述句仍在同时讲机制、堆书目、交代核查方法，判断刚出口就被括号和引用块拆掉。按方针把出处/口径/`[核 …]` 搬进本章注、把两处被出处挤掉的判断写完、删掉「读完这一章」模板句，体例即可达到样章标准。技术准确性、示例与代码、禁用词已查，未见问题。章级骨架按方针维持，不提结构项。

## 亮点（必写）
1. **§1.1 四项目循环占比表**（pi-mono / opencode / codebuff / goose 的 6.2% / 2.0% / 9.8% / 3.7%）。读者一屏内看到「循环不是系统」，且口径边界（不含 CLI/UI、opencode 测的是 V1）写在明处。抽出出处时表本身和这些数字不要改。
2. **§1.3 Guides / Sensors 两种失衡**： 「每次都要纠正三轮才对」对「AGENTS.md 写得很漂亮但没人知道有没有用」。这是本章判据真正能被工程师拿去盘点自己系统的句子，不要改成抽象定义。
3. **§1.4「system prompt 单独迁移是负收益」及其对第 7 章的约束**，加上 **§1.5 建造顺序与「不要跳过第 1 阶段」**。反面证据划了边界，第 5 段给出抄哪一步；抽出 AHE 书目时这两句判断和建造顺序表都不得改坏、不得再加升华句。

## 问题清单

- 位置：§1.1 加粗句「循环占 2% 到 10%。剩下的 90% 以上」
- 问题：判断出口立刻被长括号拆台；括号里的推算链也没有把「为什么」写完。级别 P1。
- 理由：目标读者刚读到本章主句，括号立刻把它降级为推算并重述测量/消融，视线被拆掉。同时「循环占比低 ⇒ 单改循环收益有限」只写了箭头，没写机制：行数占比低不等于那一层不值得改，缺的是「循环只是调度器，可靠性长在工具/上下文/治理上，单改循环能动的代码面太窄」。这是方针点名的标准病例，也是抽出后必须补完的判断。
- 修改建议：加粗句保留，括号整段删除。后接两句普通陈述，细节进注。可采用：「**循环占 2% 到 10%。剩下的 90% 以上，就是本书要讲的东西。** 2%–10% 是四个包的测量；「剩下的就是本书对象」是本书推算。[^1-1] 循环占比低，不等于循环不重要，等于只改循环、不动工具接口、上下文和治理，能动的代码面太窄——可靠性并不长在调度器上。这个方向与 §1.4 一致：单独换 system prompt 是负收益，工具、中间件、记忆才是正收益。」推算链、测量命令、AHE 交叉引用的出处进同一条注。保留「本书推算」这几个字，不要把推算写成测量。

- 位置：§1.1「出处 [核 2026-08-18]」整段引用块；上表证据级别列
- 问题：三组实验的判断被全文书目、URL、「三点必须交代」和核日期堵住；抽出后没告诉读者对自家系统做什么。级别 P1。
- 理由：读者要带走的是「底层模型没换、只动 harness 就有十几个百分点」。现在判断和下一块引用之间夹着作者全名、题名、两个 URL、15 vs 16、自建任务集、英文原话、arXiv。表格里「个人博客实测」也不是方针允许的级别用词。抽出之后正文停在立论，没有指向已有的建造顺序——不要另写升华句。
- 修改建议：引用块整段改成章末注（Bölük 文、Improving Deep Agents 文、Meta-Harness 三条注，后文复用）。**诚实边界全部保留进注，一条不删**：标题 15 个模型/正文 16 个、自建任务集非公开基准、厂商自述、单基准、本书未独立复现、本书取方向不取数值精度。表内证据级别列只留方针允许的几个字（「厂商自述」「论文实测」）加「未复现」；hashline 的机制解释留在单元格。表下改为一句「口径见注」。正文在「底层模型没换」之后用普通句子收束，采用：「三组实验的共同点：**底层模型没换**。选定模型之后，你还能动的东西全在 harness 里。对自家系统该怎么动，按 §1.5 的建造顺序——先隔离，再上下文，再恢复。」不要再加「由此可见 harness 很重要」类句子。

- 位置：§1.1 循环占比表下「本书测量，2026-08-17。方法：」引用块
- 问题：测量口径、排除规则、代际标注用引用块堆在表下。级别 P1。
- 理由：方针规定引用块只用于代码题注和需要保留的短引；口径/判定方法进本章注，表下只留「口径见注」。其中 opencode 测的是 V1、V2 循环在 `packages/core/`、两代并存——这是读者解释 2.0% 必须知道的机制，不是口径，抽走后正文会读不懂那一行。
- 修改建议：引用块删除。表下留「本书测量。口径见注。」注里写：统计 `.ts`/`.rs`、排除规则、同一条命令见 §1.6、pi-mono `test/` 下非 `*.test.ts` 辅助文件被计入但对结论无影响、四个包不含 CLI/UI/provider。正文另留一句机制：「opencode 那一行测的是 V1；V2 的循环在另一包，两代并存，第 2 章引用时会分开标。」不要把 V1/V2 只藏进注里。

- 位置：§1.1「厂商自述（Vivek Trivedy《The Anatomy of an Agent Harness》」
- 问题：公式旁堆了作者全名、题名、博客、日期。级别 P1。
- 理由：同一章还有另一篇 Trivedy（Improving Deep Agents 文）。全题名写在叙述句里，读者会把公式出处和表里 +13.7 pp 那篇混成一篇。方针短名：一篇「Anatomy 文」，一篇「Improving Deep Agents 文」。
- 修改建议：改为「Anatomy 文把它写成一个公式（厂商自述）。[^1-x] 同一框架被 ETCLOVG 综述用作核心论题。」公式本身保留。全题名、日期、LangChain Blog 进注，后记 C.2 已有完整条目。

- 位置：§1.1 末句「读完这一章，你能说出 harness 的七层」
- 问题：章首模板句。级别 P1。
- 理由：前言已说明每章固定结构。读者在这里要的是本章特有判断（循环占 2%–10%，剩下的是 harness），不是学习目标朗诵。七层、四象限、建造顺序分别在 §1.2 / §1.3 / §1.5，段末再报一遍菜单没有新信息。
- 修改建议：整句删除。不要改写成另一条「读完你能」。§1.1 以「选定模型之后还能动的东西全在 harness 里」收住即可；若 P1-2 已指向 §1.5，此处不要再复述建造顺序。

- 位置：§1.2.1「来自综述 *Agent Harness Engineering: A Survey*。它是 Li et al., 2026」
- 问题：首次出现即堆全题名、作者、TMLR 在审、核日期。级别 P1。
- 理由：短名规则是首次「ETCLOVG 综述」+ 注号。但「尚未通过同行评审」是诚实边界，删掉会让七层切分听上去像已发表共识，读者会按同行评审论文的权重来采用这套分层。
- 修改建议：改为「学术侧目前公开的切分里，覆盖面最广的一份是 ETCLOVG 综述（尚未通过同行评审）。[^1-x] 它把 harness 分成七层，取各层英文首字母记作 **ETCLOVG**：」全题名、Li et al.、TMLR 在审、无 arXiv、核日期进注。「尚未通过同行评审」这几个字留在正文，后文再提七层不要重说一遍。不要把这句改成听上去已定论。

- 位置：§1.2.2「Birgitta Böckeler 在 martinfowler.com 的文章里」及随后全题名、日期
- 问题：实践者文章的作者全名、站点、题名、日期、「非实证」堆在切分判断前面。级别 P1。
- 理由：读者要先知道「按谁在造分成三环、本书讲中环」。书目字段让判断迟到。「非实证」必须保留，否则三个同心圆会被读成源码普查结论。
- 修改建议：改为「Böckeler 文按谁在造分成三个同心圆（实践者文章，非实证）。[^1-x]」下列内核 / 中环 / 外环不变。「本书讲的是中环」那句判断保留。全题名、日期、martinfowler.com 进注；后文只称「Böckeler 文」。

- 位置：§1.2.3 表下「治理那一行是本书普查」整段；随后「pi-mono 用 9 个回调把全部可变性外置（`pi-mono/packages/agent/src/types.ts:149-293`）」一段
- 问题：表下整段是判定方法；叙述句里出现完整 `file:line` 和「本书核 2026-08-17」。级别 P1。
- 理由：对照表单元格里的 `file:line` 是查阅面，必须保留。表下 grep 口径和叙述句里的路径是打断。读者在这里要带走的判断是「权限判定不写在循环体内、可变性用窄接口外置」；现在判断夹在命中行号和字段列表中间。§1.6 还写「按 §1.2.3 表下那条口径注」，抽出后这条交叉引用会落空。
- 修改建议：表下改为「本书普查。口径见注。」判定方法、四个文件的命中情况（pi-mono/codebuff 零命中、opencode 16 处全是装配、goose 是注册 inspector）进注，含 `file:line`。叙述句改为：「成熟项目一致地把治理、上下文、持久化从循环里赶出去，用一组窄接口挂上。[^1-x] pi-mono 用 9 个回调把可变性外置；codebuff 用 7 个按次注入的函数让同一份循环同时跑本地和云端。」路径与「本书核」进注。goose 正在迁往 state_machine、两套并存这一句机制留正文，目录路径进注。§1.6 保鲜期表「按 §1.2.3 表下那条口径注」改为「按本章注里治理普查那条」。

- 位置：§1.3「W. Ross Ashby 在 *An Introduction to Cybernetics*（1956）中提出」至 Conant & Ashby 1970 全题名
- 问题：定律已经用白话讲清，叙述句又叠作者全名、书名、期刊、年份。级别 P1。
- 理由：读者在这一节要用定律做诊断（输出空间 vs 检查覆盖）。完整书目让「调节器多样性必须不小于系统多样性」迟到。后记 C.1 已收这两条。两条必须分开注，避免读者以为 1970 年那篇属于 1956 年的定律——正文已正确区分，抽出时不要合并成一条注。
- 修改建议：改为「多样性是一个系统能进入的不同状态的数量。Ashby 的必要多样性定律：调节器的多样性必须不小于被调节系统的多样性。[^1-x] 配套的另一条结论——调节器只能调节它拥有模型的东西——来自十四年后 Conant 与 Ashby 的另一篇论文，不属于 1956 年那本书里的定律。[^1-y]」两条方向（压缩输出空间 / 放大调节能力）和可判定形式原样保留。书名、期刊、年份进注。

- 位置：§1.4「论文全称：*Agentic Harness Engineering:…* arXiv:2604.25850」；表下「论文实测，单基准单次实验」引用块；「30 个 hard [核 2026-08-18]」
- 问题：AHE 消融的判断被全称、arXiv、口径补全、核日期堵住。级别 P1。
- 理由：本章最硬的反面证据是「system prompt 单独迁移是负收益」和「组件收益不可加」。读者需要先读到这两句，再决定要不要点进注核对 69.7% 种子和 75.3/73.0/71.9/67.4。全称和口径现在挡在表前。「往种子里加一件，不是从全量里减一件」是读表所需的机制，不是书目，必须留正文。「本书未独立复现」不得删。
- 修改建议：改为「最强的一份反面证据来自 AHE 论文。[^1-x] 它在 Terminal-Bench 2（89 个任务）上先演化 10 轮，再把演化出来的**单个**组件搬回种子 harness。方向是「往种子里加一件」，不是「从全量里减一件」。」表保留。表下引用块删除，改「论文实测，未复现。口径见注。」「**system prompt 单独迁移是负收益。**」及其对第 7 章的约束整段不动。反面证据二的难度标签改为「用的是 Terminal-Bench 2 官方榜单的难度标签（4 easy / 55 medium / 30 hard）。[^1-x]」`[核 …]` 进注。种子 69.7%、GPT-5.4 high、三个角色 agent、四个绝对百分比进同一条 AHE 注，章内复用，不要每张表再写一遍全称。

- 位置：§1.5 与 §1.6 之间（整章无此节）
- 问题：缺少 `## 本章注`，抽出的出处没有落点。级别 P1。
- 理由：方针要求本章注插在第 5 段之后、`## 1.6 版本与复核` 之前，不占五段序号。没有这一节，上面各条的「进注」无处可放；`verify-citations.sh` 扩扫后也收不到从叙述句搬走的 `file:line`。
- 修改建议：插入 `## 本章注`，用 `[^1-n]` 从 1 连续编号，同一出处一条、后文复用。一条注只干一件事（定位符 / 书目 / 口径）。建议至少覆盖：循环行数测量与 90% 推算链；Bölük 文（含 15 vs 16、自建任务集、未复现）；Improving Deep Agents 文（厂商自述、只动三个旋钮）；Meta-Harness（单基准、未复现）；Anatomy 文；ETCLOVG 综述（尚未通过同行评审、无 arXiv）；Böckeler 文（非实证）；§1.2.3 治理普查判定方法及叙述句里那几处 `file:line`；Ashby 1956 与 Conant & Ashby 1970 分两条；AHE 论文（全称、arXiv、种子口径、未复现、难度标签）。对照表单元格和 §1.6 代码块题注里的路径原样留在原处，并在对应注里再出现一次定位符，保证脚本扩扫后仍能解析。§1.6 基准块的完整书目保留，不要删掉改成「见本章注」。

- 位置：§1.1 起至 §1.5 的全部数据表；§1.2.1「下面这张图是本书的地图」；图题「图 1-1 七层执行框架」
- 问题：样章表无编号表题；图题缺冒号；用「下面这张图」相对指位。级别 P1。
- 理由：方针要求第 1 章按「表 {章}-{序号}：{描述}」补表题，正文用编号引用；图题统一带冒号；不用「上图/下图/下面这张图」——排版后位置会变。现在九张内容表都无法被「见表 1-1」指称，读者口头引用只能复述表意。
- 修改建议：在每张内容表上方加表题，章内从 1 连续。建议：表 1-1 四项目循环占比；表 1-2 三组只改 harness 的实验；表 1-3 四项目层次分布；表 1-4 确定性 vs 推理型；表 1-5 四象限例子；表 1-6 AHE 组件消融；表 1-7 演化 agent 的预测力；表 1-8 建造顺序；表 1-9 四象限盘点。首次提及改为「见表 1-N」。§1.6 两张基准/保鲜期表不纳入内容表编号。图改为 `![图 1-1：七层执行框架与本书章节的对应](../figures/ch01-1-book-map.svg)`；「下面这张图是本书的地图」改为「见图 1-1」。图后「图 1-1 的结论用文字复述：」那一段保留。

- 位置：§1.4 起句「如果「harness 决定一切」这个论断有裂缝」
- 问题：反面证据段拿来打的靶，比 §1.1 实际立的论更强。级别 P2。
- 理由：§1.1 说的是「选定模型之后，还能动的东西全在 harness 里，收益比大多数人估计的大」，并立刻声明「这不等于模型不重要」。§1.4 写成「harness 决定一切」，读者会以为本章主张过绝对命题，随后的消融像是在收回自己没说过的话。
- 修改建议：改成与 §1.1 同口径，例如「如果『模型给定之后，harness 是决定可靠性的那个变量』有裂缝，得在第一章就摆出来。」不要把 §1.1 的立论加码成「决定一切」。

- 位置：图 1-1 节点标签「工具接口与协议」「上下文与记忆」「可观测与运营」「验证与评估」
- 问题：与 §1.2.1 七层中文不完全同词。级别 P2。
- 理由：读者刚记下 ETCLOVG 的「工具接口 / 上下文管理 / 可观测性 / 验证」，图上多出「与协议 / 与记忆 / 与运营 / 与评估」，会停下来问是不是另一套分层。图的第二行已经写出覆盖的章，不需要靠加长层名来塞章节。
- 修改建议：图节点第一行与 §1.2.1 七层中文对齐（执行环境、工具接口、上下文管理、生命周期与编排、可观测性、验证、治理与安全）；章节覆盖保持在第二行。不改图的层次顺序和「模型是唯一不讨论的部分」。

## 移交校对
- §1.1 标题与加粗句：「2%–10%」与「2% 到 10%」两种写法并存，统一即可。
- §1.6 底稿格：「并当面说明这是文件时间戳」——「当面」疑为「在此」之误。
