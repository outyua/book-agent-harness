# 审稿报告：ch06-kv-cache.md（第 1 轮）

## 结论
不达标
P0：1　P1：10　P2：2

## 总体评价
本章的机制、判据和可抄交付已经写完：前缀匹配铁律、四类语义、断点骨架、热/冷时机、一次性调用净亏损，目标读者拿去就能选型。病不在内容空洞，而在三层引用还没做——叙述句同时承担判断、定位符、书目和核查笔记，读感像调研底稿。按方针把出处搬进「本章注」、判断留在正文并写完，P1 清零后即可达标。技术准确性已抽查 goose 四类语义与默认值，与正文一致；oh-my-pi TTL 默认值一条存疑，见 P0-1。术语先白话后名词、五段职责、第 4 段篇幅与反面证据、第 5 段交付物、禁用词、「不是 X 而是 Y」同节连用，均已查，未见问题。

## 亮点

1. **开篇先白话后术语。** §6.1 用「从左往右算、留下只跟前面有关的中间结果」讲完再给出 KV cache 与前缀匹配。这是本章的骨架句，也是方针给的正例。抽出出处时不要改这三句，也不要在后面加「由此可见缓存很重要」。
2. **对照表和代码题注里的 `file:line`。** 时间四家表、热/冷四家表、可观测性阶梯表，以及 goose / kilocode / openclaw / hermes-agent / codebuff 等代码块后的路径行，是查阅面，不是打断。抽出时**原样保留**，不得当成本轮问题删掉。
3. **`prefix_invariance.rs` 不贴 428 行全文。** §6.5.2 只讲命题、构造、两个断言、四个防摆设细节。这是方针点名的正例。不要为了「可抄」把测试全文贴回来。

抽出时必须保留、并让它比路径更靠前的三处判断（现在已经被定位符挤到括号里，但句子本身是对的，禁止改写方向、禁止删诚实限定）：

- 未知组合默认 `ImplicitStrict` 的「为什么」（最严格的假设对所有缓存都安全，假设没缓存会丢掉折扣）。
- 不了解服务端时不抄 Claude Code 的 1 个断点（最好的做法依赖你没有的信息）。
- 一次性调用开缓存是净亏损（付了 1.25 倍写入，等不到 0.1 倍读取）。

诚实限定（厂商自述、推算不是实测、通用 agent 不是 coding agent、回看窗口是约数、fork 可能是继承）全部保留，只换到注或后一句，禁止为读起来干净而删掉。

## 问题清单

### P0-1

- 位置：§6.3 判据四，原句「oh-my-pi 默认 1h，pi-mono（其前身）默认 5m」
- 问题：存疑。正文当作「同一团队两代产品反转默认值」的反例，但当前源码与所引注释对不上。
- 理由：底稿把出处写在 `oh-my-pi/packages/ai/src/providers/anthropic.ts:469-490`，并引用中文注释「Agent 会话经常在后台任务上空闲超过 5 分钟，5m 断点在恢复时会整前缀冷失效」。本章把路径删了、判断留下了。当前该处是 `getCacheControl`：英文注释写 Five-minute writes are the cheapest，`resolveCacheRetention(..., "short")`，中文那句不存在。`settings-schema.ts` 里 `providers.cacheRetention` 默认 `"auto"`，说明是 Anthropic 5m + keep-alive。若默认已不是 1h，「反转」故事会误导读者去抄 1h、多付一倍写入溢价。本章基准是 oh-my-pi `37eee71978`（08-16），我未能对那次快照取证，故不直接判错。
- 修改建议：移交技术作者核实。核实后：无论默认是 1h 还是 5m，定位符进本章注；判断「必须按自己真实负载实测」留正文；若快照已不是 1h，改正文反例，不要编一个已经不存在的注释。

### P1-1

- 位置：§6.5 与 §6.6 之间（第 5 段之后、`## 6.6 版本与复核` 之前）
- 问题：缺少方针要求的 `## 本章注`。叙述句里的定位符、书目、口径目前无处可放。
- 理由：目标读者要先读判断、再决定要不要点进去核。没有章末注，抽出的 `file:line` 只能继续挤在句子里，分层做不成。注不占五段序号，§6.6 不要改号。
- 修改建议：在 §6.5 后插入 `## 本章注`，用 `[^6-N]` 从 1 连续编号。同一出处一章一条、后文复用。三条模板按方针：定位符注、书目注、口径/推算注。判断不准写进注。

### P1-2

- 位置：叙述句里的完整 `path:line`（对照表、代码题注、§6.5.4 验收清单**除外**）
- 问题：定位符还在叙述句里。方针点名的 §6.2.1 MiMo-Code、§6.2.2 回看窗口、§6.3 各判据段都在。
- 理由：读者对照自己代码时，需要的是「MiMo-Code 会把代理后的 Claude 静默丢标记」这种判断；路径是核的时候才用的。句子里塞完整路径，判断被挤成定语。
- 修改建议：下列各条路径、相对行号、提交哈希从叙述句搬进本章注，正文留项目名 + 机制 + 判断 + 注号。**不要动**时间表、热/冷表、可观测性表、代码题注、§6.5.4 清单里的 `file:line`。抽出后问三句（为什么 / 不这么做会怎样 / 抄不抄），答得上就写在这家旁边，答不上保持短；禁止补「由此可见缓存很重要」。

  必须抽出的叙述句位置：

  - §6.2.1「未知组合」后的 `` `:21-22` ``、OpenAI 两端点的 `` `:34-39` ``
  - §6.2.1 MiMo-Code 句：`transform.ts:249-252` / `:253-273` / `:499-516`（坑本身留正文，并比路径更显眼）
  - §6.2.2 上限 `cache_semantics.rs:26`
  - §6.2.2 回看窗口 `transform.ts:544-570` / `:571-577`（「约 20 个块、未见官方确定值」进同一条注，判断留正文）
  - §6.2.2 Claude Code 1 个断点 `claude.ts:3063-3106`
  - §6.2.3 JSON 键序 `OAuthHTTPClient.swift:842-853`；随机 ID `run_agent.py:4952-4953` 等；枚举顺序 `prompt-cache-stability.ts:9-49`、`tools.rs:197`；运行期开关 `state.ts:226-229` / `:222-225`
  - §6.3 判据一 `` `prompts.ts:513` ``、`` `:343-347` ``
  - §6.3 判据二 `world_state/mod.rs:202-212` 等、`permissions.rs:88-124`、`client.rs:1217-1258`
  - §6.3 判据三表下 `session-maintenance.ts:147-152`、`microCompact.ts:262-270`
  - §6.3 判据四 aider / OpenMinis 各路径；goose 提交 `31d3ff2bd1`（代码题注 `anthropic.rs:432-434` 保留）
  - §6.4 失败模式三 `promptCacheBreakDetection.ts:28-49` 及 `:117-120`、`:483-489`、`:35-37`、`:573-576`
  - §6.5.1 `cache_semantics.rs:23-46` / `:44`；§6.5.2 正文里的 `prefix_invariance.rs` 全路径（题注 `:1-6` 保留）；§6.5.2 引用块里的 pi-mono `reducer.test.ts`（此块不是代码题注）
  - §6.5.3 表下 cindy `:72-76` / `:103-104`（表内 `:66-80` 保留）

  提交号与 PR 标题（`66051ec7d2` #11022、`ee2ccb5cb6` #2611、`31d3ff2bd1` #11179、`d4b1e9644e` #123543）一并进注。`verify-citations.sh` 必须能扫到注里的 `file:line`。

### P1-3

- 位置：§6.1「Manus 团队公开分享的生产数据」整段 + 紧随的出处引用块；§6.3 判据二「Manus 不动工具定义」；§6.4 反面证据二「Manus 认为这是……头号指标」
- 问题：同一篇 Manus 文在章内三次摊开出处；第一次把作者、题名、URL、存档路径、行号、联网 200、英文原文对照全部堆进正文。
- 理由：读者要的是「输入输出约 100:1，钱几乎全在预填充」和「命中率该做成常驻指标」。作者全名和存档行号挡在判断前面。三次全文违反「同一出处一条注」。
- 修改建议：建一条 `[^6-1]`，短名用「Manus 文」。注内放作者、题名、日期、厂商自述、原文只写「平均」、未给测量方法、通用 agent 不是 coding agent、后记 C.2。正文三处都只留判断 + `厂商自述` + `[^6-1]`。§6.1 那整段 `> 出处：…` 引用块删除（信息进注，不删）。100:1 的判断留正文。不要把「厂商自述 / 通用 agent」删掉，只换位置。

### P1-4

- 位置：§6.1「一个不管缓存的 agent，账单可能是管好缓存的十倍左右」
- 问题：加粗判断出口，同一句后立刻跟上整条推算链，属于方针点名的自我拆台。
- 理由：读者先要记住「差一个数量级、这是上界」。把 100:1、价差、303/33、命中率回落全塞进同一段，判断刚说完就被降级成演算草稿纸。
- 修改建议：短句保留。后一句普通陈述：「这是上界，不是实测。」证据级别写「本书推算」。推算链（100:1 来自 Manus 文、价差 $0.30 vs $3.00、输出按同价、命中率不足时比值向 1 回落、本书没有真实会话命中率）整段进注，可与 `[^6-1]` 分条。首 token 延迟随重预填充恶化这句是机制，留正文。不要用括号把短句再解释一遍。

### P1-5

- 位置：本章所有「依据本仓库 source map 还原源码，快照 2026-06-01」和逐句 `[06-01 基准]`
- 问题：Claude Code 快照声明重复约 8 次，`[06-01 基准]` 出现 9 次（含表内）。
- 理由：同一条时效信息每出现一次就打断一次。读者需要的是「Claude Code 只打 1 个 / 日期锁存 / 微压缩与 editing 互斥」；快照资格一章声明一次就够，基准块已有 commit 与日期。
- 修改建议：本章**首次**出现 Claude Code 处（§6.2.2「四个实现」那句）写一次「依据本仓库 source map 还原，快照 2026-06-01」。后文只写「Claude Code」。删掉段中反复的 source map 句和所有逐句 `[06-01 基准]`。表「项目」列改成「Claude Code」，`file:line` 仍留在出处列。各条定位符注可带 `[06-01 基准]`，正文不带。基准块那行 commit 保留。

### P1-6

- 位置：§6.4 反面证据二，从「口径注（这个 21 是哪 21 个）」到「没进这一轮的 7 个是……」整段引用块
- 问题：核查笔记当正文。判断已经被下一段口径注拆掉。
- 理由：读者要带走的是「21 家都读了 usage，几乎没人把命中率做成常驻指标」。21 是哪 21 个、判定方法、与 23/28 的关系，是核的时候才用的。段中「口径注」「判定方法两条」是核查口吻。
- 修改建议：引用块整段进本章注。正文只留判断：21 家都读了 usage，几乎没人把命中率做成常驻指标；Manus 文把命中率当作头号指标（`厂商自述` + 复用 `[^6-1]`）。注内保留：普查日期 2026-07-29、与第 7 章 §7.2.1 同一批、两条判定方法、21/23/28 不能互换、未覆盖的 7 个仓库名。抽出后核第 4 段篇幅仍不少于第 3 段一半（第 3 段的路径也会搬走，两边一起瘦）。§6.6 保鲜期表「按 §6.4 口径注」改为指向该注号。

### P1-7

- 位置：§6.1 末句「读完这一章，你能判断自己接的每个厂商……」
- 问题：全书同构开场/收束的模板句。本章已经有自己的判断句，这句是段末朗诵。
- 理由：铁律、100:1、先讲约束后讲压缩/记忆/检索，读到这里读者已经知道能做什么。再套「读完你能」与部扉页、前言的结构说明重复，读起来像填空。
- 修改建议：整句删除。不要改写成另一句学习目标。章首判断保持现有「缓存按前缀匹配……全部作废」和「所以放在上下文部分第一章」。

### P1-8

- 位置：叙述句中的 `[核 YYYY-MM-DD]`、URL、出处/口径引用块、以及两处「底稿」口吻
- 问题：核查口吻和书目字段还在正文里。
- 理由：删掉这些标记之后，句子仍应完整并下判断。它们现在是句子的语法成分，读者会被带着做复核，而不是做选择。
- 修改建议：
  - 叙述句里所有 `[核 2026-07]` / `[核 2026-08-17]` / `[核 2026-08-18]` / `[核 2026-08-19]` 删掉，日期跟对应章末注走；表单元格保留 `file:line`，`[核 …]` 记号去掉。
  - 三处 URL（Manus 文、mid-conversation 文档、Prompt caching 文档，含 §6.5.4 清单里那条）进注。清单里的规则句和 `file:line` 保留；英文原话若要留，用短引 + 注号，不要 URL。
  - §6.3 判据二文档全名改为短名（如「中途改 system / 工具」文档）+ 注号；§6.5.3 判断「`input_tokens` 只是未缓存余量」留正文，「一个必须记住的口径问题」这种核查标题改成判断句本身。
  - §6.4 失败模式一「底稿的六条纪律没有覆盖它」删掉。留 bug、固定规范顺序、推广到任何进入 prompt 的集合。提交号进注。
  - §6.4 失败模式二「底稿把……当作纯粹的正面案例。复核当前代码后要修正」改成直接判断：日期降到天与正确性有张力；hermes-agent 用时区缩写和 UTC 偏移同时满足正确与日内字节稳定。代码题注保留。

### P1-9

- 位置：§6.2.3 时间表、§6.3 判据三热/冷表、§6.5.3 可观测性表；图 6-1、图 6-2 的 alt 图题
- 问题：三张内容表没有「表 6-N：…」表题；两张图题缺冒号。
- 理由：方针本轮要求第 6 章给表编号，置于表上方；图题统一为 `图 6-N：描述`。没有编号，正文无法写「见表 6-2」，排版后「上表」也不能用。图题缺冒号是样章要定的体例，不在这章定死，后面 15 章会对不齐。
- 修改建议：表上方加表题，例如「表 6-1：时间戳如何避免破坏前缀」「表 6-2：缓存还热时各家如何推迟改上下文」「表 6-3：可观测性四阶」。正文用「见表 6-N」引用。图题改为 `![图 6-1：四类缓存语义与对应策略](../figures/ch06-1-cache-semantics.svg)`、`![图 6-2：一次请求的渲染顺序、断点位置与失效级联](../figures/ch06-2-cache-invalidation-cascade.svg)`。「图 6-N 的结论用文字复述：」已有，保留。

### P1-10

- 位置：§6.6「行内标记约定」两行；基准块「外部规格基准」里「均标 `[核 2026-07]`」等行内约定
- 问题：行内时效标记约定还写在章末，和「叙述句不再写 `[核 …]`」冲突。
- 理由：方针已裁定：第 6 章这两行删掉，改由序在样章确认后统一说明章末注与基准块的分工。留着会让改稿人继续往句子里贴 `[核]`。
- 修改建议：删除「行内标记约定：`[核 2026-08-17]` = …」整段。基准块四行保留；「外部规格基准」改成写文档名与日期，不要再吩咐正文去标 `[核]`。价格、限额、会话中途机制的核日期改由对应章末注承担。不要把基准块搬回章首。

### P2-1

- 位置：§6.2.1 开篇「显式断点派（Anthropic、AWS Bedrock）」「自动隐式派（OpenAI、Gemini、DeepSeek、Qwen、Kimi）」
- 问题：两派名单比随后的图 6-1 / goose 表更粗：goose 把 Bedrock/Vertex 限于 claude 模型，`kimi_code` 是显式断点，`moonshot` 才是隐式宽容。
- 理由：读者若按开篇名单给非 Claude 的 Bedrock 或 kimi_code 做策略，会和本章自己给出的表打架。后面「用 OpenAI 这句话不足以决定策略」已经示范了正确精度，开篇不必更粗。
- 修改建议：开篇与图 6-1 对齐即可，例如显式派写「Anthropic，以及 Bedrock / Vertex 上的 Claude」；Kimi 要么去掉、要么写成「Moonshot 隐式 / kimi_code 显式」。不要另起一套分类。

### P2-2

- 位置：§6.4 反面证据二末句「这是一个低成本高回报的缺口」
- 问题：判断已经在前一句说完（几乎没人做成常驻指标）。这句经不起「低多少、回报是什么」的追问。
- 理由：删掉它读者不损失信息。§6.5.3 第 1 级已经告诉人怎么做命中率。
- 修改建议：删句。不要改成另一句升华。

## 结构项

无。五段职责顺序、§6.6 基准块位置、章级骨架按方针维持，不建议增删或拆章。

## 移交校对

- §6.1 价差写作 `$0.30 vs $3.00`：构建链走 Temml，美元价可能被当成公式。按方针，价格不要用会进数学模式的 `$`。
- §6.2.3 表内英文引语「byte-stable for the full day」、§6.5.4 清单里的英文文档原话：抽出到注之后，核对中英文引号与句末标点是否统一。
