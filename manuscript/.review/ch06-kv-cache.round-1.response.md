# 修改说明：ch06-kv-cache.md（第 1 轮）

逐条对应报告编号：
- P0-1：移交技术作者。正文 1h/5m 反例与「必须按自己真实负载实测」未改；该句原本已无定位符，未编 `anthropic.ts:469-490` 注释。等核实后再把定位符写入本章注。
- P1-1：已修改。在 §6.5 与 §6.6 之间插入 `## 本章注`，`[^6-1]` 至 `[^6-31]`。
- P1-2：已修改。叙述句中的 `path:line`、相对行号、提交哈希全部进本章注；对照表、代码题注、§6.5.4 清单中的 `file:line` 原样保留。抽出后补了 kilocode「发标记服务端会忽略」、MiMo-Code「不要只按模型名判断」。
- P1-3：已修改。Manus 文三处收成 `[^6-1]`，正文只留判断 + `厂商自述` + 注号。§6.1 出处引用块删除（信息进注）。
- P1-4：已修改。短句「一个不管缓存的 agent，账单可能是管好缓存的十倍左右」保留，后接「这是上界，不是实测。本书推算。」推算链进 `[^6-2]`。首 token 延迟留正文。
- P1-5：已修改。快照声明只在 §6.2.2「四个实现」出现一次；后文与表「项目」列只写「Claude Code」；`[06-01 基准]` 仅留在对应定位符注。基准块 commit 行保留。
- P1-6：已修改。口径注整段进 `[^6-27]`。正文留「21 家都读了 usage，几乎没人把命中率做成常驻指标」+ Manus 文头号指标（`厂商自述` + `[^6-1]`）。§6.6 保鲜期表改指该注号。
- P1-7：已修改。删除 §6.1 末句「读完这一章，你能……」。
- P1-8：已修改。叙述句与表单元格中的 `[核 …]` 已删；三处 URL 进注；文档改短名「中途改 system / 工具」；§6.5.3 改为判断句本身；失败模式一删「底稿的六条纪律没有覆盖它」；失败模式二改为「把日期降到天，和正确性有真实张力」。
- P1-9：已修改。表 6-1 / 表 6-2 / 表 6-3 已加表题，正文用「见表 6-N」。图题改为 `图 6-1：`、`图 6-2：`。
- P1-10：已修改。删除「行内标记约定」整段。外部规格基准改为文档名与日期，不再吩咐正文标 `[核]`。
- P2-1：已修改。显式派改为「Anthropic，以及 Bedrock / Vertex 上的 Claude；kimi_code 同属此类」；隐式派 Moonshot，去掉笼统的 Kimi。
- P2-2：已修改。删除「这是一个低成本高回报的缺口」。

较大改动 3 处：§6.1 出处块删除并拆开「十倍」判断与推算链；§6.4 口径注整段搬进本章注；新增 `## 本章注` 整节。

## 移交清单

- P0-1：核 oh-my-pi 基准 commit `37eee71978` 上 TTL 默认究竟是 1h 还是 5m；核 `oh-my-pi/packages/ai/src/providers/anthropic.ts:469-490` 中文注释「Agent 会话经常在后台任务上空闲超过 5 分钟，5m 断点在恢复时会整前缀冷失效」是否存在。若快照已不是 1h，改正文反例并写入定位符注，不要编已经不存在的注释。判断「必须按自己真实负载实测」已在正文，维持。
