# 审稿报告：ch06-kv-cache.md（第 2 轮）

## 结论
达标
P0：0　P1：0　P2：0

## 修改核验（第 2 轮起）

- P0-1（oh-my-pi / pi-mono TTL 默认值存疑）：已修复。正文关键句改为「**oh-my-pi 和前身 pi-mono 默认都是 5 分钟档**，不是 1 小时档」，与技术备忘裁定一致；删掉不存在的中文注释与「两代产品反转默认值」故事；「必须按你自己的真实负载实测」仍在正文并下判断；定位符进 `[^6-32]`。原「oh-my-pi 当前默认 1h」反例已从正文消失。正文仍有一句「它曾经默认过 1 小时档……随后改回 5 分钟档加保温」，对应备忘证据 2（`b16d316aa9` → `1132c3e31c`），不是原反例，不另立案，不得把该裁定再当问题。
- P1-1（缺 `## 本章注`）：已修复。插在 §6.5 与 §6.6 之间，`[^6-1]` 至 `[^6-32]`，不占五段序号。
- P1-2（叙述句里的 `path:line`）：已修复。对照表、代码题注、§6.5.4 清单的 `file:line` 原样保留；叙述句里的路径、相对行号、提交哈希全部进注。抽出后补了 kilocode「发标记服务端会忽略」和 MiMo-Code「不要只按模型名判断」，是机制与代价，不是正确的废话。删掉注号后句子仍完整、仍下判断。
- P1-3（Manus 文三次全文）：已修复。三处正文只留判断 + `厂商自述` + `[^6-1]`；作者、题名、URL、存档、通用 agent、原文只写「平均」均在该注。
- P1-4（十倍判断自我拆台）：已修复。短句保留，后接「这是上界，不是实测。本书推算。」推算链进 `[^6-2]`。首 token 延迟留正文。
- P1-5（Claude Code 快照声明重复）：已修复。仅 §6.2.2「四个实现」声明一次；后文与表「项目」列只写「Claude Code」；`[06-01 基准]` 只在对应定位符注。基准块 commit 行保留。
- P1-6（§6.4 口径注当正文）：已修复。正文留「21 家都读了 usage，几乎没人把命中率做成常驻指标」；口径进 `[^6-27]`；§6.6 保鲜期表改指该注号。
- P1-7（「读完这一章，你能……」）：已修复。全稿已无该句。
- P1-8（叙述句 `[核 …]`、URL、底稿口吻）：已修复。叙述句与表单元格无 `[核 …]`；三处 URL 进注；文档用短名「中途改 system / 工具」；§6.5.3 是判断句本身；失败模式一已无「底稿的六条纪律」；失败模式二改为「把日期降到天，和正确性有真实张力」。
- P1-9（表无编号、图题缺冒号）：已修复。表 6-1 / 6-2 / 6-3 有加粗表题，正文「见表 6-N」；图题「图 6-1：」「图 6-2：」；图后文字复述保留。
- P1-10（行内标记约定）：已修复。该段已删。外部规格基准写文档名与日期，不再吩咐正文标 `[核]`。
- P2-1（两派名单过粗）：已修复。显式派写 Anthropic + Bedrock / Vertex 上的 Claude + kimi_code；隐式派写 Moonshot。
- P2-2（「低成本高回报的缺口」）：已修复。该句已删。
- 移交校对：价差已改为注内「0.30 美元 vs 3.00 美元 / 百万 token」，正文不再出现 `$`。已核。

亮点三处（开篇白话三句、对照表 `file:line`、`prefix_invariance` 不贴全文）及三处保护判断（`ImplicitStrict` 的为什么、不抄 1 个断点、一次性调用净亏损）均未被改坏。抽出后没有用总结句填空。

## 总体评价
第 6 章已完成引用分层：叙述句删掉注号后仍完整、仍下判断；三十二章注一条一事或同一文件多行收束，Manus 文复用 `[^6-1]`；TTL 默认值已按技术备忘改成两代都是 5 分钟档，定位符在 `[^6-32]`。诚实限定（厂商自述、这是上界不是实测、通用 agent 不是 coding agent、回看窗口是约数、fork 可能是继承）正文或后一句还在，细节在注里。五段职责、对照表 `file:line`、第 5 段四类交付物均在。技术准确性、术语、示例与代码、定位匹配、呈现与排版已查，未见问题。无未决存疑。

## 亮点（必写）
1. **§6.1 开篇白话三句**仍在：从左往右算、中间结果只跟前面有关、开头一模一样才跳过重算，然后才给 KV cache 与前缀匹配。抽出 Manus 出处块之后没有加「由此可见缓存很重要」。
2. **三张对照表和代码题注的 `file:line`** 原样保留（表 6-1 时间四家、表 6-2 热/冷四家、表 6-3 可观测性四阶）。查阅面没有被当打断删掉。
3. **§6.5.2 `prefix_invariance.rs` 仍只讲命题、构造、两个断言、四个防摆设细节**，没有把 428 行贴回来。`ImplicitStrict` 的「为什么」、不了解服务端时不抄 1 个断点、一次性调用开缓存是净亏损，三句都比路径更靠前。

## 问题清单
无。

## 结构项
无。五段职责顺序、§6.6 基准块位置、章级骨架按方针维持。
