# 审稿报告：ch06-kv-cache.md（第 3 轮）

## 结论
达标
P0：0　P1：0　P2：0

## 修改核验（第 2 轮起）

上一轮问题清单为空（P0 / P1 / P2 均为 0），无待核条目。本轮核的是 2026-08-20 裁决「源码用来讲课，不是核验」在第 6 章的落实，以及第 1 轮技术备忘与保护句是否被改回去。

- 裁决-1（页上已有代码则叙述句去掉定位符注号）：已落实。全章无 `[^6-n]`。硬指标两处：§6.2.1 goose 枚举题注已是 `cache_semantics.rs:9-18`，其后 ImplicitStrict / 两个 API 的判断句不再叠号；kilocode 白名单题注已有路径，叙述句不再叠号。对照表、题注、§6.5.4 清单里的 `file:line` 仍在原位，未回填进注。
- 裁决-2（没贴代码的叙述句不建定位符注）：已落实。MiMo-Code 代理坑、Claude Code 只打 1 个、codex 键序事故、OpenMinis 键序、hermes-agent 随机 ID、openclaw MCP 排序、Claude Code 锁存 / 互斥 / 归因、codex world state、aider / OpenMinis 保温、pi-mono `validPrefixes`，叙述句只留项目名 + 机制 + 判断。
- 裁决-3（本章注从 32 条往薄里收，可整节删除）：已落实。`## 本章注` 整节已删，无空节。21 家普查判定方法与「十倍左右」推算链在 §6.6。未把 `file:line` 回填进注。按裁决允许，不把「注没了」当问题。
- 裁决-4（100:1 写明厂商自述、通用 agent）：已落实。「Manus 文给出的生产数据（厂商自述）是输入输出 token 比约 **100:1**」后紧跟「这是通用 agent 的数，不是 coding agent 的账单。」限定在叙述句，不点注也能用对这个数。
- 裁决-5（「十倍左右」保留上界限定，推算链进 §6.6）：已落实。正文「这是上界，不是实测。本书推算。」仍在；推算链现为 §6.6「『十倍左右』的推算链」，句末无注号。
- 裁决-6（已补机制判断不得改回）：未改坏。ImplicitStrict 的为什么、不了解服务端时不抄 1 个断点、一次性调用开缓存是净亏损、回看窗口估为约 20 个块、fork 可能是继承，均在原位。
- 裁决-7（不新造核验口吻）：已落实。阅读主路径无「口径见注」「详见注」「可核」「自己去查仓库」「可以自己去核」。测量方法、grep、普查判定、推算链只在 §6.6。
- 裁决-8 / 技术备忘（oh-my-pi / pi-mono 默认都是 5 分钟档）：未改坏。正文「**oh-my-pi 和前身 pi-mono 默认都是 5 分钟档**，不是 1 小时档」维持；未写回「当前默认 1h」反例。历史「曾经默认过 1 小时档……随后改回 5 分钟档加保温」仍是备忘允许的沿革，不是原反例，不得再当问题。

第 2 轮按旧红线加进去的 32 条本章注，本轮按新裁决整节删掉——这是覆盖，不是把已修技术错误改回去。style sheet §4 / §2 / §7 / §11 已按同日裁决覆盖，文首有「源码用来讲课，不是核验」。

## 总体评价
第 6 章已按新红线把阅读主路径从核验作业改回讲课：源码摊在页上是为了讲机制（未知组合为什么默认 ImplicitStrict、一次性调用为什么是净亏损），路径留在题注和对照表，叙述句写判断和代价。100:1 与「十倍左右」的限定写在数字后面那一句，顺着读完全章不必点开任何注也能用对这两个数、决定抄不抄。TTL 默认值、开篇白话三句、不抄 1 个断点等保护句均未被改坏。技术准确性、术语与概念、结构与逻辑、示例与代码、语言表达、定位匹配、呈现与排版均已查，未见问题。无未决存疑。无 P0 / P1 / P2。

## 亮点（必写）
1. **§6.1 开篇白话三句仍在，且 100:1 / 十倍的限定已跟数字走。** 从左往右算、中间结果只跟前面有关、开头一模一样才跳过重算，然后才给 KV cache 与前缀匹配。抽出注号之后没有补「由此可见缓存很重要」；「厂商自述」「通用 agent 不是 coding agent 的账单」「这是上界，不是实测。本书推算。」都在叙述句里。不要改这三句，也不要把限定再藏回去。
2. **§6.2.1 goose 枚举是在讲课，不是布置核验。** 四类语义的代码在页上，题注给路径；紧接着用注释原文讲清未知组合默认 `ImplicitStrict` 的为什么（最严格的假设对所有缓存都安全，假设没缓存会丢掉折扣），以及同一家两个 API 分属不同类。读者不必去核 `cache_semantics.rs` 也能决定自己的默认值。不要再给这两句叠定位符。
3. **§6.5.2 `prefix_invariance.rs` 仍只讲命题、构造、两个断言、四个防摆设细节**，没有把 428 行贴回来。不了解服务端时不抄 1 个断点、一次性调用开缓存是净亏损、oh-my-pi / pi-mono 默认 5 分钟档，三句都比路径更靠前。

## 问题清单
无。

## 结构项
无。五段职责顺序、§6.6 基准块位置、章级骨架按方针维持。本章注整节删除符合「可薄，可无，不要空节」，不移交。
