# 校对报告：第 1 组（ch00、ch01、ch02、ch03、ch04、part1、part2）（第 1 校）

## 验收结论

合格（差错率约 0.34/10000，验收线 1/10000；质疑清单若有被确认为知识性/逻辑差错者，每处加计 2 个，最坏情况约 2.7/10000，须整改后重发）
直改 8 处　质疑 7 项

计错：错别字/多字 1 处 ×1 = 1.0；标点 3 处 ×0.1 = 0.3；数字形式 1 处 ×0.1 = 0.1；格式 3 处 ×0.1 = 0.3；合计 1.7。
字数口径：七个文件去掉代码块与空白后的字符数 49,729（`sed` 删围栏块后 `wc -m`），比版面字数口径更严。

编码扫描：零宽字符、BOM、控制字符、U+FFFD 均未发现；围栏均已闭合；成对括号/引号/书名号逐行配对无缺；中文句子内未发现半角逗号句号（反引号内除外）。

## 直改记录

### 错别字 / 多字
1. ch03-tools.md §3.2.4 第 2 条「防止经过压缩压缩（Minified）的单行超长代码」→「防止经过压缩（Minified）的单行超长代码」。重复字。

### 标点
2. ch03-tools.md §3.2.6 图 3-1 复述段「「未找到目标」、「多重匹配歧义」与「区间超限拒绝」」→ 删去引号之间的顿号。GB/T 15834—2011 4.5.3.5。
3. ch04-steering.md §4.2.2「（「不对」、「先看配置」、「在 config 目录下」）」→ 删去引号之间的顿号。同上。
4. ch03-tools.md §3.2.2「完成文件检索与修改。 2026-08 之后」→ 删去句号后的多余半角空格。

### 数字
5. ch01-model-plus-harness.md §1.4 反面证据一「反而导致了 -2.3 pp 的性能回归」→「−2.3 pp」。同段表 1-6 与全章其余负值均用减号 U+2212，此处为半角连字符，按多数用法统一。

### 格式
6. ch02-agent-loop.md §2.6「哪些会过期」表 goose 一行：单元格内两条命令里的 `|` 未转义，导致该行被解析为 6 列（表头 4 列）。改为 `\|`。渲染后命令文本不变（GFM 在表格单元格内以 `\|` 表示竖线）；ch04 §4.6 同表已用此写法。说明：这是本轮唯一触及反引号内文本的改动，仅为 markdown 转义，命令本身未改；若上游不接受，请退回。
7. ch01-model-plus-harness.md §1.5.3 与 §1.6 之间连续 9 个空行 → 1 个空行。
8. ch03-tools.md §3.5.3 与分隔线之间 2 个空行 → 1 个空行。

## 质疑清单（回流编辑裁决）

1. **交叉引用指向不存在的节。** ch01 §1.6、ch03 §3.6、ch04 §4.6 复核命令的注释写「未克隆先见前言《怎么拿到这些项目的代码》」，ch02 §2.6 写「后记《怎么复核这本书》D.1」。他校：ch00 前言没有《怎么拿到这些项目的代码》一节；ch99 有「D. 怎么复核这本书」。全书 13 章都用前言那条写法，只有 ch02 指向后记。文字在代码块注释内，校对未动。建议：要么在前言补该节，要么统一改成 ch02 的写法。
2. **ch04 §4.2.3 与 §4.6 的 craft-agents-oss 路径与仓库不符。** 正文写 `craft-agents-oss/packages/shared/src/llm-connections.ts:475-477`，保鲜期表写 `llm-connections.ts:476`，而 §4.6 复核命令与仓库实际路径都是 `packages/shared/src/config/llm-connections.ts`（他校：`ls projects/craft-agents-oss/packages/shared/src/`，`src/llm-connections.ts` 不存在）。反引号内路径，校对未动。建议上游补 `config/` 并复核行号。
3. **ch03 kimi-code 注册点行号前后不一致。** 表 3-3 写 `kimi-code/packages/agent-core/src/agent/tool/index.ts:796-878`，§3.6 复核脚本注释写「agent/tool/index.ts:795-876」。两处至少有一处过期。反引号/代码块内，校对未动。
4. **ch02 §2.6 保鲜期表「4940→5116 的行数」无正文对应。** §2.2.5 正文只给了 5,248 → 6,207（写作时与 2026-08-27），没有出现 4940 与 5116 两个数；表中这句像是旧稿残留。建议核对 PR #9574 前后行数并在正文补写或删去该句。
5. **ch04 §4.6 基准块 kimi-code 出现两个 commit。** 「项目 commit」行写 kimi-code `676e4d822` (08-27)，「外部规格基准」行写 kimi-code `44a6c70e66` (08-17)。若后者行号（`errors.ts:500-551`）未按 08-27 重核，应写明；否则统一。
6. **ch00 前言「表 0-1」「表 0-2」无题注。** §证据分级 段落写「表 0-1 的证据等级」，§涉及的项目 段落写「表 0-2 的『出现章数』」，但前言四张表都没有「表 0-N：」题注（style sheet §9 要求表题置于表上方）。另两张表（六部分表、症状表）是否也编号，需上游决定，校对未加。
7. **ch04 §4.3 图 4-1 复述段「直至当前完整 Run 彻底结束且会话重归空闲状态后才行激活」**：「才行激活」不成词，疑为「才被激活」或「方才激活」。属语病，校对未动。

## 未改而记录的体例观察

- 数值范围全组一律用半字线「–」（2%–10%、第 6–10 章、10,000–11,000 层），GB/T 15835 建议浪纹「～」或一字线「—」；本组 9 处、全稿一致，未按国标改，留待 style sheet 决定。
- 时效标记全稿用「[核实于 YYYY-MM-DD]」（13 处），editorial-standards §2.2 写的是「[核 YYYY-MM-DD]」（0 处）。按多数用法未动，建议 style sheet 记一条。
- ch01 表 1-2 单元格「行哈希＋新内容」用全角加号，未动（表格内）。

## style sheet 追加

本次未新立规则。上述两条体例观察待编辑裁决后再入 style sheet。
